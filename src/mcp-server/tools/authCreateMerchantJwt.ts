/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authCreateMerchantJwt } from "../../funcs/authCreateMerchantJwt.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostAuthV1CompanyAuthenticateRequest$inboundSchema,
};

export const tool$authCreateMerchantJwt: ToolDefinition<typeof args> = {
  name: "auth-create-merchant-jwt",
  description: `Create Merchant JWT`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await authCreateMerchantJwt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
