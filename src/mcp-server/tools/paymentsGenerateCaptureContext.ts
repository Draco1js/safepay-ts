/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsGenerateCaptureContext } from "../../funcs/paymentsGenerateCaptureContext.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PostOrderPaymentsV3Track7233ed61Fe5048368a84C5e17ae90f4dRequest$inboundSchema
    .optional(),
};

export const tool$paymentsGenerateCaptureContext: ToolDefinition<typeof args> =
  {
    name: "payments-generate-capture-context",
    description: `Generate Capture Context`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await paymentsGenerateCaptureContext(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
