/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportGenerateToken } from "../../funcs/passportGenerateToken.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$passportGenerateToken: ToolDefinition = {
  name: "passport-generate-token",
  description: `Generate Time-Based Token`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await passportGenerateToken(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
