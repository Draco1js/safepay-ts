/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderPaymentsCreate } from "../../funcs/orderPaymentsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostOrderPaymentsV3Request$inboundSchema.optional(),
};

export const tool$orderPaymentsCreate: ToolDefinition<typeof args> = {
  name: "order-payments-create",
  description: `Payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await orderPaymentsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
