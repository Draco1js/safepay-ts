/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansSearch } from "../../funcs/plansSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClientPlansV1SearchRequest$inboundSchema.optional(),
};

export const tool$plansSearch: ToolDefinition<typeof args> = {
  name: "plans-search",
  description: `Search Plans`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await plansSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
