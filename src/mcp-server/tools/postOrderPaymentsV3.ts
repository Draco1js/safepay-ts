/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postOrderPaymentsV3 } from "../../funcs/postOrderPaymentsV3.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostOrderPaymentsV3Request$inboundSchema.optional(),
};

export const tool$postOrderPaymentsV3: ToolDefinition<typeof args> = {
  name: "post-order-payments-v3",
  description: `Payment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postOrderPaymentsV3(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
