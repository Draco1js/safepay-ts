/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsCapture } from "../../funcs/paymentsCapture.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PostOrderPaymentsV3Track1a46dc7091f948928e250bb5205b69c7Request$inboundSchema
    .optional(),
};

export const tool$paymentsCapture: ToolDefinition<typeof args> = {
  name: "payments-capture",
  description: `Capture`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentsCapture(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
