/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersListPaymentMethods } from "../../funcs/customersListPaymentMethods.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .GetUserCustomersV1Cus2fc20245E0f8447fAf75Fa651cfd54aaWalletRequest$inboundSchema
    .optional(),
};

export const tool$customersListPaymentMethods: ToolDefinition<typeof args> = {
  name: "customers-list-payment-methods",
  description: `List Payment Methods`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customersListPaymentMethods(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
