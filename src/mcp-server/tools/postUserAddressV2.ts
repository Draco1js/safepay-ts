/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postUserAddressV2 } from "../../funcs/postUserAddressV2.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostUserAddressV2Request$inboundSchema.optional(),
};

export const tool$postUserAddressV2: ToolDefinition<typeof args> = {
  name: "post-user-address-v2",
  description: `Create Address`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postUserAddressV2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
