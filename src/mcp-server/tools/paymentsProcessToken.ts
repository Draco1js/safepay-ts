/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsProcessToken } from "../../funcs/paymentsProcessToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PostOrderPaymentsV3TrackDccdef16008e4fae81cc1b7d81bf6c44Request$inboundSchema
    .optional(),
};

export const tool$paymentsProcessToken: ToolDefinition<typeof args> = {
  name: "payments-process-token",
  description: `Process Transient Token`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentsProcessToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
