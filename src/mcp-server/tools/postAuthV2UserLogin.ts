/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postAuthV2UserLogin } from "../../funcs/postAuthV2UserLogin.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostAuthV2UserLoginRequest$inboundSchema.optional(),
};

export const tool$postAuthV2UserLogin: ToolDefinition<typeof args> = {
  name: "post-auth-v2-user-login",
  description: `Create Shopper JWT Using Password`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postAuthV2UserLogin(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
