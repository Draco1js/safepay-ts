/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { apiSettingsGetApiKey } from "../../funcs/apiSettingsGetApiKey.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$apiSettingsGetApiKey: ToolDefinition = {
  name: "api-settings-get-api-key",
  description: `Find Api Key`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await apiSettingsGetApiKey(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
