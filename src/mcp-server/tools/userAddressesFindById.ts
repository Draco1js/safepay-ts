/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userAddressesFindById } from "../../funcs/userAddressesFindById.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$userAddressesFindById: ToolDefinition = {
  name: "user-addresses-find-by-id",
  description: `Find Address`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await userAddressesFindById(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
