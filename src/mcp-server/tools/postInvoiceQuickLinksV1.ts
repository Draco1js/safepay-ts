/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postInvoiceQuickLinksV1 } from "../../funcs/postInvoiceQuickLinksV1.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostInvoiceQuickLinksV1Request$inboundSchema.optional(),
};

export const tool$postInvoiceQuickLinksV1: ToolDefinition<typeof args> = {
  name: "post-invoice-quick-links-v1",
  description: `Create Quick Link`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postInvoiceQuickLinksV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
