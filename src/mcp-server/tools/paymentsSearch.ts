/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsSearch } from "../../funcs/paymentsSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetReporterApiV1PaymentsRequest$inboundSchema.optional(),
};

export const tool$paymentsSearch: ToolDefinition<typeof args> = {
  name: "payments-search",
  description: `Search Payments`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
