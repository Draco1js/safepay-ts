/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getClientPlansV1Search } from "../../funcs/getClientPlansV1Search.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClientPlansV1SearchRequest$inboundSchema.optional(),
};

export const tool$getClientPlansV1Search: ToolDefinition<typeof args> = {
  name: "get-client-plans-v1-search",
  description: `Search Plans`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getClientPlansV1Search(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
