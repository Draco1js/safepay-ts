/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { putClientApiSettingsV1 } from "../../funcs/putClientApiSettingsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$putClientApiSettingsV1: ToolDefinition = {
  name: "put-client-api-settings-v1",
  description: `Update Api Key`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await putClientApiSettingsV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
