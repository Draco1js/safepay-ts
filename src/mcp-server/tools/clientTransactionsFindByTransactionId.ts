/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientTransactionsFindByTransactionId } from "../../funcs/clientTransactionsFindByTransactionId.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$clientTransactionsFindByTransactionId: ToolDefinition = {
  name: "client-transactions-find-by-transaction-id",
  description: `Find Transaction`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await clientTransactionsFindByTransactionId(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
