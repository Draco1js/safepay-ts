/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { plansUpdate } from "../../funcs/plansUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PutClientPlansV1PlanD4869a7800364d6697bd6afeb5282bcdRequest$inboundSchema
    .optional(),
};

export const tool$plansUpdate: ToolDefinition<typeof args> = {
  name: "plans-update",
  description: `Update Plan`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await plansUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
