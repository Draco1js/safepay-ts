/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getUserMetaV2Country } from "../../funcs/getUserMetaV2Country.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserMetaV2CountryRequest$inboundSchema.optional(),
};

export const tool$getUserMetaV2Country: ToolDefinition<typeof args> = {
  name: "get-user-meta-v2-country",
  description: `Country`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getUserMetaV2Country(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
