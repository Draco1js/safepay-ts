/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userWalletsListPaymentMethods } from "../../funcs/userWalletsListPaymentMethods.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$userWalletsListPaymentMethods: ToolDefinition = {
  name: "user-wallets-list-payment-methods",
  description: `List Payment Methods`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await userWalletsListPaymentMethods(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
