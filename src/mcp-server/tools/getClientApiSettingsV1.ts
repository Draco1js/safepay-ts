/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getClientApiSettingsV1 } from "../../funcs/getClientApiSettingsV1.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$getClientApiSettingsV1: ToolDefinition = {
  name: "get-client-api-settings-v1",
  description: `Find Api Key`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await getClientApiSettingsV1(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
