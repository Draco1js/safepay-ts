/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metaListCountries } from "../../funcs/metaListCountries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserMetaV2CountriesRequest$inboundSchema.optional(),
};

export const tool$metaListCountries: ToolDefinition<typeof args> = {
  name: "meta-list-countries",
  description: `Countries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metaListCountries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
