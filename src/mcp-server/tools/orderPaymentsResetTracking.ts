/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { orderPaymentsResetTracking } from "../../funcs/orderPaymentsResetTracking.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$orderPaymentsResetTracking: ToolDefinition = {
  name: "order-payments-reset-tracking",
  description: `Reset`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await orderPaymentsResetTracking(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
