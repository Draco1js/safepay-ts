/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postUserV1Guest } from "../../funcs/postUserV1Guest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostUserV1GuestRequest$inboundSchema.optional(),
};

export const tool$postUserV1Guest: ToolDefinition<typeof args> = {
  name: "post-user-v1-guest",
  description: `Create Guest JWT`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postUserV1Guest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
