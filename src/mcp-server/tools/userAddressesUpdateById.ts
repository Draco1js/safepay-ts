/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userAddressesUpdateById } from "../../funcs/userAddressesUpdateById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PutUserAddressV2Address5ce54f87A8234da689907b26d048ce00Request$inboundSchema
    .optional(),
};

export const tool$userAddressesUpdateById: ToolDefinition<typeof args> = {
  name: "user-addresses-update-by-id",
  description: `Update Address`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userAddressesUpdateById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
