/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postUserCustomersV1 } from "../../funcs/postUserCustomersV1.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostUserCustomersV1Request$inboundSchema.optional(),
};

export const tool$postUserCustomersV1: ToolDefinition<typeof args> = {
  name: "post-user-customers-v1",
  description: `Create Customer`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postUserCustomersV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
