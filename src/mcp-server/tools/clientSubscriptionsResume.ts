/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientSubscriptionsResume } from "../../funcs/clientSubscriptionsResume.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PutClientSubscriptionsV1Sub130aa5b290564c8eB9c99149e68f17ceResumptionRequest$inboundSchema
    .optional(),
};

export const tool$clientSubscriptionsResume: ToolDefinition<typeof args> = {
  name: "client-subscriptions-resume",
  description: `Resume Subscription`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await clientSubscriptionsResume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
