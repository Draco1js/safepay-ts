/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getUserCustomersV1 } from "../../funcs/getUserCustomersV1.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserCustomersV1Request$inboundSchema.optional(),
};

export const tool$getUserCustomersV1: ToolDefinition<typeof args> = {
  name: "get-user-customers-v1",
  description: `List Customers`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getUserCustomersV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
