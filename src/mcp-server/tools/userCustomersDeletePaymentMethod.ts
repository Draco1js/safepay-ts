/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userCustomersDeletePaymentMethod } from "../../funcs/userCustomersDeletePaymentMethod.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$userCustomersDeletePaymentMethod: ToolDefinition = {
  name: "user-customers-delete-payment-method",
  description: `Delete Payment Method`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await userCustomersDeletePaymentMethod(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
