/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { shoppersCreateSafepay } from "../../funcs/shoppersCreateSafepay.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostUserV2Request$inboundSchema.optional(),
};

export const tool$shoppersCreateSafepay: ToolDefinition<typeof args> = {
  name: "shoppers-create-safepay",
  description: `Create Safepay Shopper`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await shoppersCreateSafepay(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
