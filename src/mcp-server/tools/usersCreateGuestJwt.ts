/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersCreateGuestJwt } from "../../funcs/usersCreateGuestJwt.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostUserV1GuestRequest$inboundSchema,
};

export const tool$usersCreateGuestJwt: ToolDefinition<typeof args> = {
  name: "users-create-guest-jwt",
  description: `Create Guest JWT`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersCreateGuestJwt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
