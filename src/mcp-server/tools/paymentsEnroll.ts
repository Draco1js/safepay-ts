/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsEnroll } from "../../funcs/paymentsEnroll.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PostOrderPaymentsV3TrackF3adbae6640247fe948bEe82244ac372Request$inboundSchema
    .optional(),
};

export const tool$paymentsEnroll: ToolDefinition<typeof args> = {
  name: "payments-enroll",
  description: `Enrollment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentsEnroll(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
