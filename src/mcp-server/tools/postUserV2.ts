/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postUserV2 } from "../../funcs/postUserV2.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostUserV2Request$inboundSchema.optional(),
};

export const tool$postUserV2: ToolDefinition<typeof args> = {
  name: "post-user-v2",
  description: `Create Safepay Shopper`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postUserV2(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
