/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postV1CompanyLogin } from "../../funcs/postV1CompanyLogin.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$postV1CompanyLogin: ToolDefinition = {
  name: "post-v1-company-login",
  description: `Login Company`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await postV1CompanyLogin(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
