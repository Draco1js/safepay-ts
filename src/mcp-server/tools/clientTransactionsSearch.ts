/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientTransactionsSearch } from "../../funcs/clientTransactionsSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClientTransactionsV1SearchRequest$inboundSchema
    .optional(),
};

export const tool$clientTransactionsSearch: ToolDefinition<typeof args> = {
  name: "client-transactions-search",
  description: `Search Transactions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await clientTransactionsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
