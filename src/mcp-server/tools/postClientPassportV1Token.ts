/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postClientPassportV1Token } from "../../funcs/postClientPassportV1Token.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$postClientPassportV1Token: ToolDefinition = {
  name: "post-client-passport-v1-token",
  description: `Generate Time-Based Token`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await postClientPassportV1Token(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
