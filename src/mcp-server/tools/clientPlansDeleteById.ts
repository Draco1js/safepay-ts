/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientPlansDeleteById } from "../../funcs/clientPlansDeleteById.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$clientPlansDeleteById: ToolDefinition = {
  name: "client-plans-delete-by-id",
  description: `Archive Plan`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await clientPlansDeleteById(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
