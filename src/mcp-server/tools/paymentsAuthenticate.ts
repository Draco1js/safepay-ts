/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsAuthenticate } from "../../funcs/paymentsAuthenticate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations
    .PostOrderPaymentsV3TrackA6f9b00c8a254cbf95f9Cd6bc440eca6Request$inboundSchema
    .optional(),
};

export const tool$paymentsAuthenticate: ToolDefinition<typeof args> = {
  name: "payments-authenticate",
  description: `Payer Authentication Setup`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await paymentsAuthenticate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
