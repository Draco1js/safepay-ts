/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getClientTransactionsV1Search } from "../../funcs/getClientTransactionsV1Search.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetClientTransactionsV1SearchRequest$inboundSchema
    .optional(),
};

export const tool$getClientTransactionsV1Search: ToolDefinition<typeof args> = {
  name: "get-client-transactions-v1-search",
  description: `Search Transactions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getClientTransactionsV1Search(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
