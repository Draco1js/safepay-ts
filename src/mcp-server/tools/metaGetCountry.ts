/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metaGetCountry } from "../../funcs/metaGetCountry.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserMetaV2CountryRequest$inboundSchema.optional(),
};

export const tool$metaGetCountry: ToolDefinition<typeof args> = {
  name: "meta-get-country",
  description: `Country`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metaGetCountry(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
