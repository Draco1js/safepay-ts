/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getUserMetaV2Countries } from "../../funcs/getUserMetaV2Countries.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetUserMetaV2CountriesRequest$inboundSchema.optional(),
};

export const tool$getUserMetaV2Countries: ToolDefinition<typeof args> = {
  name: "get-user-meta-v2-countries",
  description: `Countries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getUserMetaV2Countries(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
