/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postClientPlansV1 } from "../../funcs/postClientPlansV1.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostClientPlansV1Request$inboundSchema.optional(),
};

export const tool$postClientPlansV1: ToolDefinition<typeof args> = {
  name: "post-client-plans-v1",
  description: `Create Plan`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postClientPlansV1(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
