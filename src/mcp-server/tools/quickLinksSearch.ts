/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { quickLinksSearch } from "../../funcs/quickLinksSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInvoiceQuickLinksV1Request$inboundSchema.optional(),
};

export const tool$quickLinksSearch: ToolDefinition<typeof args> = {
  name: "quick-links-search",
  description: `Search Quick Links`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await quickLinksSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
